define([
  // 'jquery',
  'underscore',
  'backbone',
], function( _, Backbone) {

	'use strict';
	return function (Module) {
		console.log('Controlbar Model');

		var Controlbar = Backbone.Model.extend({

		});
		return Controlbar;
	}
});