'use strict';

define([
  'jquery',
  'underscore',
  'backbone',
  'modules/wordpress/collections/Articles',
], function($, _, Backbone, ArticlesCollection) {

	console.log('Articles View (List)');

	var ArticlesView = new Backbone.View.extend({
		
	});

	return ArticlesView;
});